#!/bin/sh

remote_name="${1:-unknown}"
remote_url="${2:-}"

case "$remote_url" in
  *github.com*|github:*)
    printf '%s\n' \
      "Push blocked: GitHub pushes are disabled for this repository." \
      "Remote: $remote_name" \
      "Destination: $remote_url" >&2
    exit 1
    ;;
esac

exit 0
